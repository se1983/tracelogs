// TODO: Implement TcpEndpoint
// TcpAdapter should be the single entrypoint
// all Adapters should provide a very thin layer of abstraction and use the Tcp endpoint
// $Logsource -> implements adapter --> talks to tcp endpoint -> implements tokenizer ->  implements persistence
